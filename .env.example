# Public site URL — used for canonical, OG, sitemap and robots.
NEXT_PUBLIC_SITE_URL=https://pixelmorph.app

# Railway sets PORT automatically. For local dev, defaults to 3000.
# PORT=3000

NODE_ENV=production
